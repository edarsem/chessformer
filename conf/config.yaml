defaults:
  - model: small
  - data: local
  - train: default
  - _self_

paths:
  data_dir: data
  checkpoints_dir: checkpoints
  processed_dir: ${paths.data_dir}/processed

wandb:
  project: chessformer
  entity: null   # override via env WANDB_ENTITY or CLI
  enabled: true

# Overridable at CLI for eval and play scripts
checkpoint: null
split: val
mode: human_vs_ai
human_side: white
ai_elo: 2800
white_elo: null   # defaults to ai_elo if null
black_elo: null   # defaults to ai_elo if null
temperature: 1.0
display: ascii
port: 5174
open_browser: true
eval_batch_size: 512   # override on CLI: eval_batch_size=2048

# Eval head preprocessing
eval_pgn_path: data/raw_pgn/lichess_db_standard_rated_2014-09.pgn
eval_max_games: 5000  # games WITH eval annotations to collect
