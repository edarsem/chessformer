# ~54M param model, target for GPU runs (13 layers = memory-optimal on 2x T4 at batch 1024)
d_model: 512
n_heads: 8
n_layers: 12
ffn_mult: 4
dropout: 0.1
max_seq_len: 41   # 1 meta (additive) + 5 conditioning + max 32 pieces + 3 move suffix
