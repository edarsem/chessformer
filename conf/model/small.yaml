# ~2M param model, runs comfortably on MPS
d_model: 128
n_heads: 4
n_layers: 4
ffn_mult: 4       # ffn hidden dim = d_model * ffn_mult
dropout: 0.1
max_seq_len: 80   # 32 pieces + meta tokens + a few move tokens
